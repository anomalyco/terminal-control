---
"@kitlangton/terminal-control": minor
---

Add a persistent, reattachable Ghostty-backed workspace with named windows, a clickable activity-aware top-or-bottom tab strip, movable running panes, weighted recursive layouts, reversible pane zoom, connected faint dividers, tmux-familiar human controls, pane-local mouse forwarding, retrying attachment resize synchronization, semantic CLI and MCP window/layout/focus/resize/zoom/close operations, per-pane layout startup commands, globally stable pane geometry discovery, hidden-window targeting, composed workspace recording, pane/window/workspace PNG capture through MCP, inherited outer-terminal colors, synchronized damage-based rendering, immediate named screen reads, running-only session discovery, explicit stale/exited pruning, and actionable occupied-workspace errors.
